import Model from "../../app/models/Item";

describe("ItemModel", () => {
  let item;
  beforeEach(() => {
    // FIXME mock canvas in tests
    //item = new Model();
  });
  it("is something", () => {
    expect({}).toBeDefined();
  });
});
