import { describe, beforeEach, it, expect } from "vitest";

import { Item } from "./Item";

describe("ItemModel", () => {
  let item;
  beforeEach(() => {
    // FIXME mock canvas in tests
    //item = new Model();
  });
  it("is something", () => {
    expect({}).toBeDefined();
  });
});
