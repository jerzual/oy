import wrappedEntityStore, {EntityStore} from '../../app/stores/EntityStore'
import entityActions from '../../app/actions/EntityActions'
import { expect } from 'chai'

describe('EntityStore', () => {
  it('listens for an enter action', () => {

  })
})
